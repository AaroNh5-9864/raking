% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipe.R
\name{calibrate}
\alias{calibrate}
\title{Add a continuous mean calibration}
\usage{
calibrate(model, formula, tol = 0)
}
\arguments{
\item{model}{A \code{raking_model}.}

\item{formula}{A formula \code{mean(var) ~ value}, captured unevaluated.}

\item{tol}{Tolerance band half-width (>= 0), in the variable's own units.
\code{tol = 0} (default) matches the mean exactly; \code{tol > 0} allows the achieved
mean within +/- tol of the target, pulled toward it by the model \code{loss}.}
}
\value{
The updated \code{raking_model}.
}
\description{
Add a continuous mean calibration
}
\examples{
raking() |> calibrate(mean(income) ~ 58000)
raking() |> calibrate(mean(age) ~ 40, tol = 0.5)
}
