% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/losses.R
\name{losses}
\alias{losses}
\alias{EqualityLoss}
\alias{LeastSquaresLoss}
\alias{InequalityLoss}
\alias{KLLoss}
\alias{L1Loss}
\alias{BandedLoss}
\title{Loss functions}
\usage{
EqualityLoss(target = integer(0))

LeastSquaresLoss(target = integer(0), diag_weight = 1)

InequalityLoss(target = integer(0), lower = integer(0), upper = integer(0))

KLLoss(target = integer(0), scale = 0.5)

L1Loss(target = integer(0))

BandedLoss(
  inner = NULL,
  target = integer(0),
  lower = integer(0),
  upper = integer(0)
)
}
\arguments{
\item{target}{Numeric vector of target values the constraint should match.}

\item{diag_weight}{Numeric scalar or vector of per-element weights
(\code{LeastSquaresLoss}; default 1).}

\item{lower, upper}{Numeric vectors giving the lower/upper offsets of the
allowed band around \code{target} (\code{InequalityLoss}).}

\item{scale}{Numeric scale factor on the KL term (\code{KLLoss}; default 0.5).}

\item{inner}{Inner soft loss object that a \code{BandedLoss} wraps and clips into
the band.}
}
\description{
Each loss is an S7 class describing one constraint: a \code{target} to hit and a
proximal operator the solver calls each iteration. We build them up one at a
time in this file.
}
\section{Functions}{
\itemize{
\item \code{EqualityLoss()}: Exact-match loss. Drives the weighted statistic to equal
the target exactly; its proximal operator simply returns the target.

\item \code{LeastSquaresLoss()}: Least-squares loss. Penalizes squared deviation from the
target, optionally weighted per element by \code{diag_weight}.

\item \code{InequalityLoss()}: Inequality (range) loss. Allows the weighted statistic to
sit anywhere in \verb{[target + lower, target + upper]} at no cost; its prox
clips into that band.

\item \code{KLLoss()}: Kullback-Leibler loss. Matches a target distribution in
KL divergence; \code{scale} weights the term (default 0.5, as in regrake).

\item \code{L1Loss()}: L1 (absolute-deviation) loss. Robust matching; its
proximal operator is soft-thresholding around the target.

\item \code{BandedLoss()}: Penalized-band loss. Wraps a soft \code{inner} loss (KL/L2/L1)
and a hard band \verb{[lower, upper]}: its prox runs the inner soft prox (which
pulls toward the target) and then clips the result into the band. This is
the loss \code{fit()} builds for a margin with \code{tol > 0}: the soft loss prefers
the exact target, while the band is a hard wall the achieved value cannot
cross. (A \code{tol = 0} margin uses \code{EqualityLoss} instead, since a zero-width
band is just the target point.)

}}
