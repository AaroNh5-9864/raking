% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control.R
\name{rake_control}
\alias{rake_control}
\title{Solver control settings}
\usage{
rake_control(
  maxiter = 5000,
  rho = 50,
  margin_tol = 1e-04,
  eps_abs = 1e-05,
  eps_rel = 1e-05,
  verbose = FALSE
)
}
\arguments{
\item{maxiter}{Maximum ADMM iterations.}

\item{rho}{ADMM penalty parameter.}

\item{margin_tol}{Margin-based convergence tolerance. Internally converted to
a raw ADMM tolerance scaled by problem size: \verb{eps = margin_tol / sqrt(m + 2n)},
giving roughly constant margin accuracy regardless of size. Set to \code{NULL}
to use raw \code{eps_abs}/\code{eps_rel}. Supplying \code{eps_abs}/\code{eps_rel} without
\code{margin_tol} sets it to \code{NULL} automatically.}

\item{eps_abs, eps_rel}{Raw ADMM residual tolerances (used when
\code{margin_tol = NULL}).}

\item{verbose}{Print residuals while solving.}
}
\value{
A \code{rake_control} list of settings.
}
\description{
Solver control settings
}
\examples{
rake_control(maxiter = 1000)
# use raw residual tolerances instead of margin_tol:
rake_control(eps_abs = 1e-7, eps_rel = 1e-7)
}
