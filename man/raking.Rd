% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{raking}
\alias{raking}
\title{Create a raking model}
\usage{
raking(loss = "KL", name = "wt")
}
\arguments{
\item{loss}{Discrepancy measure: one of "KL", "L1", "L2", "chi_square".
Default "KL".}

\item{name}{Output weight-column name. Default "wt".}
}
\value{
A \code{raking_model} object.
}
\description{
Starts a data-free weighting model. Extend it with pipe steps
(\code{penalty()}, \code{bounds()}, and later \code{target()}/\code{calibrate()}), then \code{fit()}
it to data.
}
\examples{
data <- data.frame(sex = c("M", "F", "M", "F"))
model <- raking() |>
  target(data.frame(sex = c("M", "F"), p = c(0.5, 0.5))) |>
  penalty(entropy())
fit <- model |> fit(data)
weights(fit)
summary(fit)
}
