% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regularizers.R
\name{regularizers}
\alias{regularizers}
\alias{ZeroRegularizer}
\alias{EntropyRegularizer}
\alias{KLRegularizer}
\alias{SumSquaresRegularizer}
\title{Regularizers}
\usage{
ZeroRegularizer()

EntropyRegularizer(limit = Inf)

KLRegularizer(prior = integer(0), limit = Inf)

SumSquaresRegularizer(base = numeric(0))
}
\arguments{
\item{limit}{Optional upper cap (> 1) on how far a weight may stray from
uniform (\code{EntropyRegularizer}, \code{KLRegularizer}); default \code{Inf}.}

\item{prior}{Positive numeric reference distribution (\code{KLRegularizer}).}

\item{base}{Numeric base/target weights for \code{SumSquaresRegularizer}; an empty
vector (default) shrinks toward zero.}
}
\description{
Regularizers keep the optimized weights "regular" (close to uniform, not
extreme). Each is an S7 class implementing \code{\link[=prox]{prox()}}.
}
\section{Functions}{
\itemize{
\item \code{ZeroRegularizer()}: Identity regularizer: no penalty, prox is a no-op.

\item \code{EntropyRegularizer()}: Negative-entropy regularizer. Pushes weights toward
uniform. Optional \code{limit} (> 1) caps how far any weight can stray from
uniform; the default \code{Inf} means no cap.

\item \code{KLRegularizer()}: KL regularizer: pulls weights toward a given
\code{prior} distribution rather than uniform.

\item \code{SumSquaresRegularizer()}: Sum-of-squares regularizer: squared deviation from
the base weights \code{base} (doc 6.3). With the default empty \code{base} it shrinks
toward zero; \code{fit()} sets \code{base} to the (uniform or design) base weights.

}}
