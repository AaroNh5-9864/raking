% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipe.R
\name{target}
\alias{target}
\title{Add a categorical margin target}
\usage{
target(model, df, tol = 0)
}
\arguments{
\item{model}{A \code{raking_model}.}

\item{df}{A data.frame with one or more variable columns plus exactly one of
\code{p} (proportions) or \code{n} (counts).}

\item{tol}{Tolerance band half-width (>= 0). \code{tol = 0} means exact matching;
\code{tol > 0} allows each level within +/- tol of its target.}
}
\value{
The updated \code{raking_model}.
}
\description{
Add a categorical margin target
}
\examples{
raking() |> target(data.frame(age = c("Y", "O"), p = c(0.4, 0.6)))
# joint margin with counts, matched within +/- 0.02:
joint <- data.frame(sex = c("M", "M", "F", "F"), age = c("Y", "O", "Y", "O"),
                    n = c(110, 140, 130, 120))
raking() |> target(joint, tol = 0.02)
}
